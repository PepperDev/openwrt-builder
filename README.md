# OpenWRT Builder

Inspired by [this project](https://github.com/danpawlik/openwrt-builder) but actually thought to be automated, without human interaction.

In order to start the build process run:

```
./build
```

On success the image will be generated in the directory `output`.
